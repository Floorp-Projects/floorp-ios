{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Floorp release candidate evidence",
  "description": "Binds a release candidate to its exact source, version/build authority, signing identity, entitlements, canonical archive tree digest, IPA digest, dSYM inventory, and App Store Connect identity. Produced by collect-floorp-release-evidence.sh and validated by validate-floorp-release-evidence.py.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "archive_only",
    "source_sha256",
    "marketing_version",
    "build_number",
    "bundle_id",
    "team_id",
    "archive_path",
    "archive_sha256",
    "ipa_path",
    "ipa_sha256",
    "signing_identity",
    "archive_info",
    "ipa_info",
    "entitlements",
    "dsym_inventory",
    "app_store_connect_build_id",
    "ci_run_url",
    "xcresult_path",
    "export_status"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "archive_only": {
      "type": "boolean"
    },
    "source_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "marketing_version": {
      "type": "string",
      "minLength": 1
    },
    "build_number": {
      "type": "string",
      "minLength": 1
    },
    "bundle_id": {
      "type": "string",
      "minLength": 1
    },
    "team_id": {
      "type": "string"
    },
    "archive_path": {
      "type": "string",
      "pattern": "^/[^\\u0000-\\u001f\\u007f]*$"
    },
    "archive_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "ipa_path": {
      "type": "string",
      "pattern": "^$|^/[^\\u0000-\\u001f\\u007f]*$"
    },
    "ipa_sha256": {
      "type": "string",
      "pattern": "^([0-9a-f]{64})?$"
    },
    "signing_identity": {
      "type": ["string", "null"]
    },
    "archive_info": {
      "type": "object",
      "additionalProperties": false,
      "required": ["marketing_version", "build_number", "bundle_id", "team_id"],
      "properties": {
        "marketing_version": {"type": "string", "minLength": 1},
        "build_number": {"type": "string", "minLength": 1},
        "bundle_id": {"type": "string", "minLength": 1},
        "team_id": {"type": "string"}
      }
    },
    "ipa_info": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["marketing_version", "build_number"],
      "properties": {
        "marketing_version": {"type": "string", "minLength": 1},
        "build_number": {"type": "string", "minLength": 1}
      }
    },
    "entitlements": {
      "type": "object",
      "additionalProperties": {
        "type": ["string", "boolean", "array", "number", "null"]
      }
    },
    "dsym_inventory": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["uuid", "path"],
        "properties": {
          "uuid": {
            "type": "string",
            "pattern": "^[0-9A-F]{32}$"
          },
          "path": {
            "type": "string",
            "pattern": "^/.+\\.dSYM/Contents/Resources/DWARF/[^/]+$"
          }
        }
      }
    },
    "app_store_connect_build_id": {
      "type": ["string", "null"],
      "pattern": "^[A-Za-z0-9._-]+$"
    },
    "ci_run_url": {
      "type": ["string", "null"],
      "pattern": "^https://github\\.com/[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+/actions/runs/[1-9][0-9]*$"
    },
    "xcresult_path": {
      "type": ["string", "null"]
    },
    "export_status": {
      "type": ["string", "null"],
      "pattern": "^(ready-for-upload|uploaded-and-processed)$"
    }
  }
}
